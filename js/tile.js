function Tile(tiles)
var value === this.tile
else
.tile.tile-22 self.actuate // Activate the 22

{
if tile.merged === true;
self.tile moved;
else
self.tile;

send Tile(Grid) // Sends tiles to the grid
this.tile === true

