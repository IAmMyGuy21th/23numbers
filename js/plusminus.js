function Plus(1,2) // 1 is a used plus, 2 is not used
var value === Math.sum;
this.plus === 1;
else
var value === Math.random() < 0.5 ? 1 : 2;
this.plus === 2;

function Minus(1,2) // 1 is a used minus, 2 is not used
var value === Math.subtract;
this.minus === 1;
else
var value === Math.random() < 0.5 ? 1 : 2;
this.minus === 2;
