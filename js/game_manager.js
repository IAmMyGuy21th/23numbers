function GameManager(NewGame) // Activates this script when the game starts
{
this.rolled === false;
this.tile === 1,1,2;
this.grid === true;
this.plus === false;
this.minus === false;
this.22 === false }

// Basic tile merging
{
if tile.merged === true;
thistile.remove === value1,value2;
this.tile === false;
thistile.add === Math.next;
this.tile === true;
this.grid === true }

// When tiles aren't merged
{
if tile.merged === false;
var player.value === next }

// Receiving the 22
{
if player.tile === Math.consecutive;
this.22 === true;
var value === 2(Math.random()) Dice // Allows going after 21
else
this.rolled === false }

// The mighty 23
{
if this.tile === 23;
var self.win === true;
var end === true;
this.merged === true; }
